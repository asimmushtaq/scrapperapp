<h1> WEB SCRAPPER APPLICATION </h1>

<h1>This is the data returned by Nokogiri which does not give us react based dynamic loading </h1>

<%= @filtered_result%>

<h2>So Now we are loading data using Watir by opening up the page through chrome driver and getting them</h2>

<%= @scrapped_data.to_yaml%>


